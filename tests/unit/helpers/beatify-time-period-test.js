
import { beatifyTimePeriod } from 'spendings-tracker/helpers/beatify-time-period';
import { module, test } from 'qunit';

module('Unit | Helper | beatify time period');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = beatifyTimePeriod([42]);
  assert.ok(result);
});

